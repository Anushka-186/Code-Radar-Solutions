#include <stdio.h>

int main() {
    int num, n;
    scanf("%d", &num);
    scanf("%d", &n);
    int anushkaaaaaaaaa= 1 << n;
    if (num = num ^ anushkaaaaaaaaa) {
        printf("%d", num);
    }
    else {
    printf("0");
    }

    return 0;
}
