#include <stdio.h>

int main() {
    int num, n, bitValue;
    scanf("%d", &num);
    scanf("%d", &n);
    if (bitValue = (num >> n) & 1){
    printf("1");}
    else {
        printf("0");
    }
    return 0;
}
